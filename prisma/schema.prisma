datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model Lead {
  id        Int      @id @default(autoincrement())
  name      String
  phone     String
  eventType String
  date      String
  createdAt DateTime @default(now())
}
